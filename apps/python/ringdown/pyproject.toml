[project]
name = "calle-ringdown"
version = "0.1.0"
description = "On-call escalation that gets a human on the phone and proves the acknowledgement on a second channel."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=9.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
