# CI runs this exact file. Keep the selection to the rules that catch mistakes rather than
# taste: a lint gate nobody can reproduce locally is a gate nobody runs before pushing.
lint.select = ["E4", "E7", "E9", "F"]
